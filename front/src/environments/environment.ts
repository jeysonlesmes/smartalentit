import { PATHS as BASE_PATHS } from "./environment-paths";

export const environment = {
  production: false
};

export const baseApiUrl = 'http://localhost:3000/api/';
export const PATHS = BASE_PATHS;